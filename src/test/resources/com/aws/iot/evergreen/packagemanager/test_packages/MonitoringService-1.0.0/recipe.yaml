---
RecipeTemplateVersion: '2020-01-25'
ComponentName: MonitoringService
Description: Test recipe for Evergreen packages
Publisher: Me
Version: '1.0.0'
Parameters:
  - name: TestParam
    value: TestVal
    type: String
Lifecycle:
  all:
    install:
      skipif: onpath git
      script: sudo apt-get install git
    run: git status
  debian:
    install:
      skipif: onpath git
      script: sudo apt-get install git
  macos:
    install:
      skipif: onpath git
      script: brew install git
    run: git status
Artifacts:
  all:
    - Uri: %s/MonitoringService-1.0.0/monitor_artifact_100.txt
Dependencies:
  all:
    Log:
      VersionRequirements: '2.0'
    Cool-Database:
      VersionRequirements: '1.0'
  macos:
    mac-log:
      VersionRequirements: '1.0'

