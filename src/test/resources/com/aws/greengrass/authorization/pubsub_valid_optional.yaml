---
services:
  main:
    dependencies:
      - mqtt
      - ServiceName
    lifecycle:

  mqtt:
    lifecycle:
    parameters:
      accessControl:
        aws.greengrass.ipc.pubsub:
          policyId2:
            operations:
              - publish
              - subscribe

  ServiceName:
    resources:
      - http-resource
    lifecycle:
      run:
        windows:
          powershell -command sleep 100
        posix:
          sleep 100
    parameters:
      accessControl:
        aws.greengrass.ipc.pubsub:
          policyId1:
            operations:
              - publish
              - subscribe
