---
services:
  main:
    dependencies:
      - mqtt
      - ServiceName
    lifecycle:

  mqtt:
    lifecycle:

  ServiceName:
    resources:
      - http-resource
    lifecycle:
      run:
        windows:
          powershell -command sleep 100
        posix:
          sleep 100
    parameters:
      accessControl:
        '{"aws.greengrass.ipc.pubsub":[{"policyId1":{"resources":["/topic/1/#","/longer/topic/example/","*"]}}]}}'
