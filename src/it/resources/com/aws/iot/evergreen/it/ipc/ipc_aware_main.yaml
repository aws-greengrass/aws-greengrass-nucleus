---
services:
  plain:
    startup:
      all: "{platform.invoke} {args}"

  main:
    dependencies:
      - IPCService
    startup:
      mvn -q exec:java -f "$POM_DIR/pom.xml" -Dexec.mainClass="com.aws.iot.evergreen.it.ipc.sample.SampleIpcAwareServiceMovesToRunning" -Dexec.classpathScope="test"

