---
RecipeFormatVersion: '2020-01-25'
ComponentName: {{Mosquitto}}
ComponentDescription: Mosquitto MQTT Server
ComponentPublisher: Eclipse Foundation
ComponentVersion: '0.9.0'
ComponentDependencies:
  {{GreenSignal}}:
    VersionRequirement: "=1.0.0"
Manifests:
  - Platform:
      os: darwin
    Lifecycle:
      startup: echo "Hello this is mosquitto getting started"
      install: echo "Hello This is mosquitto being installed"
      shutdown: echo "By mosquitto signing off!"

  - Platform:
      os: linux
    Lifecycle:
      startup: echo "Hello this is mosquitto getting started"
      install: echo "Hello This is mosquitto being installed"
      shutdown: echo "By mosquitto signing off!"
