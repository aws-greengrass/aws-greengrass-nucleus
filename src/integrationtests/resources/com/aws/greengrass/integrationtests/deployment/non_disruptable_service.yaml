services:
  aws.greengrass.Nucleus:
    parameters:
      runWithDefault:
        posixUser: nobody

  main:
    dependencies:
      - nondisruptable
      - aws.greengrass.Nucleus
  nondisruptable:
    lifecycle:
      install:
