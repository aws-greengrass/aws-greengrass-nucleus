services:
  aws.greengrass.Nucleus:
    parameters:
      runWithDefault:
        posixUser: nobody

  main:
    lifecycle:
      posix:
        run:
          echo "Running main" && sleep 100
      windows:
        run:
          powershell -command echo "Running main"; sleep 100
    setenv:
      HELLO: "Hi"
