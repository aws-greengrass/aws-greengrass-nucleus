---
services:
  aws.greengrass.Nucleus:
    parameters:
      runWithDefault:
        posixUser: nobody

  main:
    lifecycle:
      install:
        all: echo All installed
    dependencies:
      - aws.greengrass.Nucleus
