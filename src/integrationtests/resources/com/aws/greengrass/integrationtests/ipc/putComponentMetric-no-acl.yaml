services:
  aws.greengrass.Nucleus:
    configuration:
      runWithDefault:
        posixUser: nobody
        windowsUser: integ-tester

  main:
    dependencies:
      - aws.greengrass.foo

  aws.greengrass.foo:
    lifecycle:
      run:
        windows:
          powershell -command sleep 1
        posix:
          sleep 1
